// "use server";
// import React from 'react';

// const formatProgress = (percent: number) => <span style={{ color: percent === 100 ? 'black' : 'initial' }}>{percent}%</span>;

// export default formatProgress;